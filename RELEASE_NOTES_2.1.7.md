# Cinemagraphy 2.1.7 — وضعیت فعلی

تاریخ: 2026-08-15

## خلاصه
نسخهٔ پایدار مسیر ۲.۱ با لندینگ، شخصی‌سازی منیفست، راهنما، کاتالوگ خارجی (از جمله انیمه با لود غیرمسدودکننده) و بهبود پروایدرهای ایرانی.

## تغییرات اصلی از ۲.۰
- صفحهٔ `/configure` و مسیر `/c/<config>/manifest.json`
- فیلتر واقعی پروایدر با `ENABLED_PROVIDERS`
- `/guide` دوزبانه
- ویترین TMDB روی لندینگ + لینک `web.stremio.com`
- کاتالوگ انیمه: timeout جدا + کش پس‌زمینه
- دنیای سریال: match بهتر؛ URL فقط از `DONYAYESERIAL_BASEURL`
- DigiMovie / PeepBoxTv در Configure قفل (به‌زودی)

## یادداشت استقرار
- `DONYAYESERIAL_BASEURL` را روی دامنهٔ فعلی سایت تنظیم کنید.
- `CATALOG_ANIME_MANIFEST_URL` لینک کامل `manifest.json` کاتالوگ انیمه باشد.
- پس از اولین درخواست، کاتالوگ انیمه ممکن است در درخواست بعدی در منیفست ظاهر شود (کش پس‌زمینه).
